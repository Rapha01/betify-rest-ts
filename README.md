# Betify REST API

Node.js REST API using TypeScript, Express, and PostgreSQL.

## Development

```
docker compose -f docker-compose.yml -f docker-compose.dev.yml up --attach api
```

## Production

```
docker compose -f docker-compose.yml -f docker-compose.prod.yml up --attach api
```
